<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{formTitle}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">{{formTitle}}</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/person/new" method="post" class="space-y-4">
            {{#isEditing}}
            {{! Campo oculto para pasar el ID en modo edición }}
            <input type="hidden" name="id" value="{{person.id}}">
            {{/isEditing}}

            <div>
                <label for="name" class="block text-gray-700 text-sm font-medium mb-1">Nombre completo:</label>
                <input type="text" id="name" name="name" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                       value="{{person.name}}"> {{! Agregamos 'value' }}
            </div>
            <div>
                <label for="dni" class="block text-gray-700 text-sm font-medium mb-1">DNI:</label>
                <input type="text" id="dni" name="dni" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                       value="{{person.dni}}"> {{! Agregamos 'value' }}
            </div>
            <div>
                <label for="birth_date" class="block text-gray-700 text-sm font-medium mb-1">Fecha de nacimiento:</label>
                <input type="date" id="birth_date" name="birth_date"
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                       value="{{person.birthDate}}"> {{! Agregamos 'value' }}
            </div>
            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                {{#isEditing}}Actualizar Persona{{/isEditing}}
                {{^isEditing}}Registrar Persona{{/isEditing}}
            </button>
            <div class="text-center mt-4">
                <a href="/dashboard" class="text-blue-600 hover:underline text-sm">Volver al Dashboard</a>
            </div>
        </form>
    </div>
</body>
</html>