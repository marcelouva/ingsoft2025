<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{#profile}}Editar Perfil{{/profile}}{{^profile}}Crear Perfil{{/profile}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">
            {{#profile}}Editar Perfil de Usuario{{/profile}}
            {{^profile}}Crear Perfil de Usuario{{/profile}}
        </h1>


        <form action="{{#profile}}/profile/edit{{/profile}}{{^profile}}/profile/new{{/profile}}" method="post" class="space-y-4">
            {{#profile}}
            <input type="hidden" name="id" value="{{profile.id}}">
            <input type="hidden" name="user_id" value="{{profile.user_id}}">
            {{/profile}}

            <div>
                <label for="email" class="block text-gray-700 text-sm font-medium mb-1">Correo Electrónico:</label>
                <input type="email" id="email" name="email" required
                       value="{{profile.email}}"
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <label for="name" class="block text-gray-700 text-sm font-medium mb-1">Nombre Completo:</label>
                <input type="text" id="name" name="name" required
                       value="{{profile.name}}"
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <label for="dni" class="block text-gray-700 text-sm font-medium mb-1">DNI:</label>
                <input type="text" id="dni" name="dni" required
                       value="{{profile.dni}}"
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                {{#profile}}Actualizar Perfil{{/profile}}
                {{^profile}}Crear Perfil{{/profile}}
            </button>
            <div class="text-center mt-4">
                <a href="/" class="text-blue-600 hover:underline text-sm">Volver al inicio</a>
            </div>
        </form>
    </div>
</body>
</html>